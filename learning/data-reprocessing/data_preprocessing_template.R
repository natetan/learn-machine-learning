# Yulong Tan
# 10.31.17

# Using libraries that are installed
# install.package('dplyr') <- this is only done once per machine, and can be done within the console
library(dplyr) # using the library dplyr
